using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ex_40 : MonoBehaviour
{
    /*
     * Crea un Raycast que nos diga en qué objeto de la escena
     * hemos hecho clic.
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
